<!-- Thanks for contributing! For security fixes, please follow SECURITY.md instead. -->

## What & why

<!-- What does this change and what problem does it solve? -->

## Checklist

- [ ] `npm run typecheck` passes
- [ ] `npm test` passes (added/updated tests where it makes sense)
- [ ] `src/pure.ts` kept side-effect-free (if touched)
- [ ] Bumped `version` (package.json + `EXTENSION_VERSION` in server.ts) and added a `changelog.md` entry, if this affects the packaged extension
- [ ] Updated README/docs if behavior or settings changed
